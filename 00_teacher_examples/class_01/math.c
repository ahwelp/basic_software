#include <math.h>
#include <stdio.h>

int main(void)
{
    double d = 8;

    printf("sqrt = %f \n", sqrt(d)); // raiz quadrada
    printf("log  = %f \n", log(d));  // logaritmo natural

    return 0;
}

